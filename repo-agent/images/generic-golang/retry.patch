diff --git a/packages/core/src/utils/retry.ts b/packages/core/src/utils/retry.ts
index a45ba0c0b..9f8eac652 100644
--- a/packages/core/src/utils/retry.ts
+++ b/packages/core/src/utils/retry.ts
@@ -205,7 +205,11 @@ export function isRetryableError(
     return status === 429 || status === 499 || (status >= 500 && status < 600);
   }

-  return false;
+  console.log(
+    `Falling through to brute-force retry for error of type ${typeof error}. Message: ${error instanceof Error ? error.message : String(error)}`,
+    error,
+  );
+  return true;
 }

 /**
@@ -232,7 +236,6 @@ export async function retryWithBackoff<T>(
     : {};

   const {
-    maxAttempts,
     initialDelayMs,
     maxDelayMs,
     onPersistent429,
@@ -250,8 +253,7 @@ export async function retryWithBackoff<T>(
     ...cleanOptions,
   };

-  const getCurrentMaxAttempts = () =>
-    getAvailabilityContext?.()?.policy.maxAttempts ?? maxAttempts;
+  const getCurrentMaxAttempts = () => 100;

   let attempt = 0;
   let currentDelay = initialDelayMs;
